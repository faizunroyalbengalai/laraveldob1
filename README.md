# laraveldob1

Laravel project generated by UDAP.

## Local dev
```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan serve
```

Visit `/` for the welcome page, `/health` for a JSON health check.
